import React from 'react';

const Resolve = ({resolve}) => {
    return (
        <div className='my-3' >
            <p className=' rounded-lg bg bg-sky-200 font-bold my-5'>{resolve.title}</p>
        </div>
    );
};

export default Resolve;